# Virtuoso — Roadmap

> **Virtuoso is a practice-studio plugin for the FeedBack host** — it generates guitar/bass (piano/drums in scope) practice routines and plays them back inside the plugin. It relaunched at **v0.1.0** as a clean-history rebrand of a mature codebase (formerly developed under another name; the full pre-0.1.0 development history is preserved in the original private archive repo). This file is the authoritative **"shipped vs planned"** record and the cross-session handoff log — **read it at session start.**
>
> Architecture + conventions live in `CLAUDE.md` (the constitution) and `docs/`; the user-facing feature list is in `README.md`. Don't duplicate those here.

## Where this is (v0.1.0)

Virtuoso ships a mature, working feature set inherited at relaunch — **this is not a greenfield 0.1.0**, it's an established plugin renamed and re-homed:

- **Four-mode DAW shell** — **Ladder** (curated skill pathways), **Custom** (full manual config), **Workout** (timed multi-block sessions), **Jam** (play-along over a procedural backing band). One shared shell; **contained playback** (Virtuoso owns its own transport/clock/Web-Audio + pitch tracker — no host-player handoff).
- **Generation engine** — the host- and DOM-independent core (`screen.js` §1–§10): scales/modes, arpeggios, chords, rhythm/technique drills, riff-vocabulary, CAGED/3NPS/Open shapes, the voicing engine, and the procedural backing band (29 genre styles via `STYLE_PALETTES`→`ARRANGEMENT_RECIPES`). The **chart** is the portable artifact.
- **Four renderers** — 3D Highway (borrowed host viz), 2D Highway, Tab, Notation.
- **Grading** — consumes the host **note_detect** verifier (the *consume-the-host-judge* rule — no parallel grader). Plus hand-marks, the proof-loop, XP/leveling/badges, custom tunings, sample-bank instruments, and the amp/cab "Rig".
- **Audio** — self-hosted WebAudioFont GM sampler + sampled electric-DI + in-browser amp/cab IRs under a master limiter; optional host `nam_tone` borrow for the live input tone.

Verification: 18 Playwright smoke suites (`.claude/skills/run-virtuoso/`, `npm test`) + the in-`screen.js` startup guards. **Smoke 18/18 green at relaunch.**

## Open threads

- **⚠ Name not yet cleared — gating for any PUBLIC launch.** "Virtuoso" hasn't passed a trademark / domain / store-name screen, and it's a common music word (likely crowded in classes 9 / 41 / 42). The private 0.1.0 id cutover is done; run a USPTO/EUIPO TM screen + `.com`/`.app` + App/Play store-name + a dictation test **before** any public launch. (A further rename would be another `virtuoso`-id data migration, so clear the name before promoting widely.)
- **Host-org governance.** Canonical repo now lives in the **got-feedback** org as `feedBack-plugin-virtuoso`. Settle with Byron how FeedBack bundles it (read-only mirror / submodule vs. user-installed), and retire the stale pre-rebrand plugin fork still in that org. **`main` HEAD is a published artifact** — FeedBack's `update_manager` zips the default-branch HEAD on update, so keep WIP on a branch, land on `main` only when green, and bump `plugin.json` **and** `VIRTUOSO_VERSION` together on every shippable change.
  - **Bundle swap + first-class menu status — LANDED 2026-06-22 (both MERGED together).** (1) **`feedBack-desktop#31`** (squash `4d19398`) — `build-common.sh` bundled-plugins list `slopscale-fork` → `virtuoso`. (2) **`feedBack#554`** (squash `ea25cfe`) — `static/v3/shell.js` NAV + `PROMOTED_PLUGINS` slot `slopscale` → `virtuoso`, plus the same dead-id cleanup on the Plugins-gallery `CURATED` map + README/inventory doc refresh (review follow-up). Admin-merged together (CI was the org Actions **billing** block, not the code; diffs verified + topkoa-approved). Now shipping to end users via the host `update_manager`. **Remaining cleanup:** (a) **`feedback-plugin-slopscale-fork`** — ✅ **RETIRED 2026-06-22**: repo now 404s (deleted by org admin, superseding the archive we'd requested; the deprecation banner `2c2e65a` + issue `#2` went with it). (b) ✅ **CONFIRMED 2026-06-23 (HOST CHECK):** `feedback-desktop/scripts/build-common.sh:165` lists `got-feedback/feedback-plugin-virtuoso` in the bundled-plugins array; `feedback/static/v3/shell.js` has the first-class nav entry (`:43`) + `PROMOTED_PLUGINS` slot (`:54`); and the built `desktop-app/resources/slopsmith/plugins/virtuoso/` payload exists (id `virtuoso`). **Zero `slopscale` references remain** in the build list or nav. The bundled snapshot is 0.1.0 (predates the 0.1.1 ship) — harmless: the `update_manager` user-dir copy (main HEAD = 0.1.1) wins over the bundled fallback, and the next desktop rebuild re-clones 0.1.1.
- **Beta channel.** `scripts/cut-beta.mjs` is re-tokened (`virtuoso` / `virtuoso_beta` / `virtuoso-beta`, source default `virtuoso-dev`). To use it, create the `virtuoso-dev` working branch and decide whether the relaunch wants a beta channel yet (`docs/beta-testing.md`).
- **Detector forward-watch.** Our outbound contributor events are `virtuoso:tier:unlocked` / `virtuoso:progress` (nothing consumes them yet). If the host ships progression spec 011 with a listener, coordinate the event names then. The note_detect verify / contained-verifier path is current and green; upstream note_detect is now **1.15.3** and the contained-verifier API surface Virtuoso calls is **unchanged** (safe target — verified 2026-06-22). **Installed note_detect — updated 2026-06-22 → 1.15.3** (was pinned at 1.10.0, whose `slopsmith/` remote had 404'd post-migration; repointed origin → `got-feedback/feedback-plugin-notedetect`, now on `main` @ `2ae9ef4`). The contained-verifier API is now present, so the desktop grades via the **preferred timing-aware contained verifier**, not the `setVerifyTarget` fallback. Detector-DSP "is contained strictly better for our credit semantics" remains `notedetect-expert`'s call.
- **Host-overlap sweep — 2026-06-22 (no action needed).** Host `feedBack#558` (global *autoplay & auto-exit*; exposes `window.slopsmith.holdAutoExit()` / `setReturnScreen()` / read-only `autoplayExit`) + companion note_detect `#31`/`#32` (→ 1.15.3) swept against Virtuoso — **zero impact.** Virtuoso uses contained playback: it never calls `window.playSong`, never emits/handles `song:ended`/`song:ready`, and uses none of the new APIs, so neither autoplay nor auto-exit can fire from it. Confirmed by the feedback-compatibility agent (clones refreshed via `C:\dev\feedback\update-all.ps1`; 3 pulled incl. note_detect → 1.15.3, 0 failed). Standing reminder: this is the recurring **host-release overlap sweep** — re-run on each new FeedBack release.
- **Carry-over by-ear dogfood (from pre-relaunch):** the classic synth-palette voices; the "grading feels a touch tight" tunable (the silence floor / timing window); and desktop note_detect needs the input device selected in the FeedBack desktop audio settings (a native-input gotcha, not a code bug).
- **Local (post-relaunch) — DONE 2026-06-22.** Pruned `.claude/settings.local.json`'s allow-list of dead slopscale-era paths (`run-slopscale`, `/api/plugins/slopscale/…`, the old `desktop\slopsmith-plugin-slopscale` repo path, `/tmp/slopscale`, the old memory-project dir): **731 → 572** allow entries (159 dead/dup removed; the 3 current `run-virtuoso` entries kept; backup at `settings.local.json.bak`). File is gitignored (local-only).
- **Repo relocated locally (2026-06-22).** Working copy moved out of the sibling-repos collection (`C:\dev\feedback\repos\feedback-plugin-virtuoso`) to a standalone `C:\Dev\feedback-plugin-virtuoso`. Git/remote/branch-tracking and all scripts are self-locating (`launch.ps1` `$PSScriptRoot`, `cut-beta.mjs` `import.meta.url`) — unaffected. The one thing the move broke: the dev-host junction `%LOCALAPPDATA%\Slopsmith\plugins-dev\virtuoso` still pointed at the old path. **Gotcha for any future move:** `launch.ps1` leaves an existing junction *as-is*, so it won't self-heal — re-point it (`cmd /c rmdir` the link, then `mklink /J` to the new path), or delete it so the next launch recreates it. Fixed + verified: smoke **18/18** green from the new location.
- **Dev-env parity (2026-06-23).** `run-virtuoso` now pins the mainline sibling versions in `env-reference.json`, and `launch.ps1` runs a startup **preflight** (asserts siblings vs reference + note_detect's contained-verifier API + a single `id:virtuoso`; CRITICAL drift aborts, borrow-drift WARNs) and **prunes** foreign/shadow plugins from `plugins-dev`. Root cause of the recurring "detection differs from the game / green notes don't fire" ghosts was a non-reproducible hand-assembled env — including a retired `slopscale` link that shadowed the repo under `id:virtuoso@0.1.0` (deleted + backed up 2026-06-23). Also added a real-desktop dogfood junction (`…\Slopsmith\plugins\virtuoso` → repo) for true mainline parity. **ROOT CAUSE FOUND (2026-06-23):** the canonical desktop (`C:\Dev\feedback\desktop-app\feedback.exe`, which bundles note_detect 1.15.3 + highway_3d 3.26.0) loads plugins from its userData dir (`%APPDATA%\slopsmith-desktop\plugins`), and the `virtuoso` symlink there was **stale** — still pointing at the pre-relocation `C:\Dev\feedback\repos\…` clone (kept at committed HEAD by `update-all.ps1`), so the real desktop showed *committed* code and **never working-tree edits** = the "my edits don't show / behaves differently" ghost. New **`launch-desktop.ps1`** self-heals that link + relaunches `feedback.exe` clean (kills the legacy-Slopsmith zombie server that bumps the port 18000→18001). Full repeatable runbook: **`docs/local-dogfood.md`**. **Lead RESOLVED (2026-06-23):** the green-note-flare delta was the `slopsmithViz_`→`feedBackViz_` host-global rename (no alias) — on the game's highway_3d 3.26.0 Virtuoso fell back to 2D and lost the host's native flare. Fixed host-aware in `screen.js` §12, and the run-virtuoso default host is now the FeedBack checkout (3.26.0). See STOPPED HERE. **Cleanup:** the orphaned `repos\feedback-plugin-virtuoso` clone still exists (now unreferenced) — drop it from `update-all.ps1` / delete to avoid future confusion.

## Session ritual

**At start** — read the above + check Open threads / any **STOPPED HERE** marker. Project memory (`MEMORY.md` index) loads automatically; verify anything a memory names still exists before acting on it.

**At end** — move finished items here; log new **Open threads** or a **STOPPED HERE** handoff. Write durable *decisions* to project memory. Keep `CLAUDE.md` / `AGENTS.md` in sync. If `screen.js` changed, run `node --check screen.js` + the smoke suite. Commit working changes in Conventional-Commits style; **commit/push only when asked.**

## STOPPED HERE (2026-06-23 — run-virtuoso retargeted to FeedBack; green-note ghost ROOT-CAUSED + fixed in code)

Two linked sessions today. **(A)** dev env made reproducible (detail below). **(B)** the `run-virtuoso` harness was **retargeted from the legacy Slopsmith host to the real FeedBack host**, which surfaced and FIXED the actual *code* cause of the "green notes don't fire" ghost — it was not only the env.

- **GREEN-NOTE GHOST RESOLVED — a host-global rename, fixed in Virtuoso.** FeedBack's Slopsmith→FeedBack re-chrome renamed the viz-factory global `slopsmithViz_<id>`→`feedBackViz_<id>` with NO alias (highway_3d **3.26.0** = the version the game ships). Virtuoso polled the old name → `borrowHostViz` timed out → silent fall back to the in-tree 2D renderer, which has no host highway listening for `notedetect:hit`, so the **native green hit-flare never rendered** (credit/scoring was fine underneath — `ptCreditWindow` still scored; only the visual was lost). The dev host pinned to highway_3d **3.22.0** (still `slopsmithViz_`) is exactly why it worked in dev, not the game.
- **Full `slopsmith*`→`feedBack*` rename sweep (feedback-compatibility agent + main thread):** `slopsmithViz_*` and `slopsmithDesktop` had **NO alias** (breaks: 3D highway; desktop level-meter/wake-lock/sink); `window.slopsmith` (host aliases it, `app.js:2371`) and `slopsmithMinigames` (host dual-publishes) **worked**; `localStorage['slopsmith_notedetect']` **unchanged** (note_detect kept its key — correct). Fix: `screen.js` §12 host-aware accessors `hostBus()`/`hostMinigames()`/`hostDesktop()` + `vizFactoryFor()` (new name first, legacy fallback) → runs on FeedBack AND legacy Slopsmith.
- **Anti-recurrence guards:** `borrowHostViz` logs a loud `[Virtuoso host-viz] … contract drift?` when a viz script loads but registers under no known global (smoke-renderers fails on it); new **`smoke-host-surface.mjs`** asserts the bus + scoring SDK + the 3D-highway ATTACH (status != fallback) on the current host (auto-discovered by run-all); `loadScriptOnce` removes a 404'd `<script>` so a legit absent-plugin fallback stays quiet.
- **Harness:** `launch.ps1` default `$Checkout` → `C:\dev\feedback\repos\feedback` (was the legacy Slopsmith clone); auto-completes first-run onboarding via `/api/profile` (FeedBack's `#v3-onboarding` modal otherwise blocks the driver/smoke); `env-reference.json` re-captured (minigames 0.1.0→0.1.1); `SKILL.md`/`driver.mjs` updated.
- **Shippable:** `plugin.json` + `VIRTUOSO_VERSION` bumped **0.1.0 → 0.1.1**; `node --check` clean; **smoke 19/19 green** vs FeedBack (renderers 7/7 with the 3D highway restored; new host-surface 5/5). WIP on `virtuoso-dev` — **not committed** (awaiting the word). Durable detail: memory `project_feedback_rechrome_globals`.

### (A) Dev-env reproducibility (earlier today)

The recurring "detection differs from the game / my edits don't show" half of the ghosts was a non-reproducible, hand-assembled local environment.

- **ROOT CAUSE:** the canonical desktop (`C:\Dev\feedback\desktop-app\feedback.exe`) loads plugins from `%APPDATA%\slopsmith-desktop\plugins\<id>` symlinks (user copy wins over bundled); virtuoso's symlink was **stale** — pointing at the pre-relocation `repos\` clone (kept at committed HEAD by `update-all.ps1`), so the desktop showed *committed* code, never working-tree edits. Proven via a reversible nonce probe. (`$env:SLOPSMITH_PLUGINS_DIR` is only the *headless* host's lever; the desktop ignores it.)
- **Shipped in-repo (committed + pushed, `main` == `virtuoso-dev` == `be40db0`):** `launch-desktop.ps1` (self-heals the userData symlink + clean-relaunches feedback.exe) + `docs/local-dogfood.md`; the `launch.ps1` env preflight + prune + `env-reference.json` (asserts the headless host's siblings vs the mainline reference — CRITICAL drift aborts, borrow-drift warns); roadmap/CLAUDE/AGENTS corrections; `.claude/settings.local.json` allow-list pruned 731→572. `screen.js` untouched (no smoke run needed); `node --check` clean.
- **Multi-plugin dev tooling (LOCAL, at `C:\Dev\feedback\` — NOT in this repo):** `author-plugin.ps1`, `new-plugin.ps1`, registry-driven `update-all.ps1`, `authored-plugins.txt`, and the `DEV-WORKFLOW.md` guide. Workspace (authored) vs `repos\` (mirrors) split; one host (`feedback.exe`). Full layout in memory **`feedback-dev-workspace-layout`**.
- **Cleanup:** orphan `repos\feedback-plugin-virtuoso` deleted → exactly one live virtuoso copy; desktop symlink points at it.

**NEXT:** (1) ✅ **DONE** — the `feedBackViz` fix + rename sweep + 0.1.1 bump committed (`bddeb7e`) and pushed to `virtuoso-dev` **and** `main` (shipping to users via `update_manager`). (2) ✅ **DONE** — HOST CHECK confirmed Byron's desktop build bundles + nav-promotes `virtuoso` (see the Host-org governance thread (b) above). (3) **name/trademark clearance** (gating for any public launch) — still open. (4) optional: dogfood the restored 3D green flare on the real desktop via `launch-desktop.ps1` (needs a mic — the one path that shows the actual green). (5) pick a first plugin to author with the `C:\Dev\feedback\` tooling. *(The green-note A/B is RESOLVED — root cause was the viz-global rename, fixed in code, not a dev-host version bump. Four-Pillar leftovers shipped/verified.)*
